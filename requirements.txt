# ============ 下载 ============
yt-dlp>=2024.1,<2027

# ============ 字幕提取（OCR）============
opencv-python>=4.12,<5
rapidocr-onnxruntime>=1.4,<2

# ============ 英文转写（Whisper）============
faster-whisper==1.2.1
# huggingface_hub 用 httpx 下载模型；SOCKS 代理环境需 socksio
httpx[socks]>=0.27,<1

# ============ 音频/视频处理 ============
pydub==0.25.1
# numpy 是 opencv/rapidocr 的间接依赖，显式锁定
numpy>=2.2,<3

# ============ 豆包朗读 ============
requests>=2.32,<3
segno>=1.6,<2
websockets>=13,<18

# ============ 封面图生成 ============
Pillow>=10,<12

# ============ 开发 ============
pytest>=8,<9
